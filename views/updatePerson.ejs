<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Person</title>
    <link rel="stylesheet" href="/updatePerson.css">
</head>
<body>
    <nav>
        <a href="/person">Back to All Persons</a>
        <a href="/">Home</a>
    </nav>
    <div class="container">
        <h1>Edit Person</h1>
        <form action="/person/update/<%= person._id %>" method="post">
            <div>
                <label for="name">Name:</label>
                <input type="text" id="name" name="name" value="<%= person.name %>" required>
            </div>
            <div>
                <label for="age">Age:</label>
                <input type="number" id="age" name="age" value="<%= person.age %>" required>
            </div>
            <div>
                <label for="work">Work:</label>
                <select id="work" name="work" required>
                    <option value="chef" <%= person.work === 'chef' ? 'selected' : '' %>>Chef</option>
                    <option value="waiter" <%= person.work === 'waiter' ? 'selected' : '' %>>Waiter</option>
                    <option value="manager" <%= person.work === 'manager' ? 'selected' : '' %>>Manager</option>
                </select>
            </div>
            <div>
                <label for="mobile">Mobile:</label>
                <input type="number" id="mobile" name="mobile" value="<%= person.mobile %>" required>
            </div>
            <div>
                <label for="email">Email:</label>
                <input type="email" id="email" name="email" value="<%= person.email %>" required>
            </div>
            <div>
                <label for="address">Address:</label>
                <input type="text" id="address" name="address" value="<%= person.address %>">
            </div>
            <div>
                <label for="salary">Salary:</label>
                <input type="number" id="salary" name="salary" value="<%= person.salary %>" required>
            </div>
            <button type="submit">Update Person</button>
        </form>
    </div>
</body>
</html>
