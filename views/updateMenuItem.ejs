<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Menu Item</title>
    <link rel="stylesheet" href="/updateMenuItem.css">
</head>
<body>
    <nav>
        <a href="/menu">Back to All Menu Items</a>
        <a href="/">Home</a>
    </nav>
    <div class="container">
        <h1>Edit Menu Item</h1>
        <form action="/menu/update/<%= menuItem._id %>" method="post">
            <div>
                <label for="name">Name:</label>
                <input type="text" id="name" name="name" value="<%= menuItem.name %>" required>
            </div>
            <div>
                <label for="price">Price:</label>
                <input type="number" id="price" name="price" value="<%= menuItem.price %>" required>
            </div>
            <div>
                <label for="taste">Taste:</label>
                <select id="taste" name="taste" required>
                    <option value="sweet" <%= menuItem.taste === 'sweet' ? 'selected' : '' %>>Sweet</option>
                    <option value="sour" <%= menuItem.taste === 'sour' ? 'selected' : '' %>>Sour</option>
                    <option value="spicy" <%= menuItem.taste === 'spicy' ? 'selected' : '' %>>Spicy</option>
                </select>
            </div>
            <div>
                <label for="is_drink">Is Drink:</label>
                <input type="checkbox" id="is_drink" name="is_drink" <%= menuItem.is_drink ? 'checked' : '' %>>
            </div>
            <div>
                <label for="ingredients">Ingredients:</label>
                <input type="text" id="ingredients" name="ingredients" value="<%= menuItem.ingredients.join(', ') %>">
            </div>
            <div>
                <label for="num_sales">Number of Sales:</label>
                <input type="number" id="num_sales" name="num_sales" value="<%= menuItem.num_sales %>" required>
            </div>
            <button type="submit">Update Menu Item</button>
        </form>
    </div>
</body>
</html>
